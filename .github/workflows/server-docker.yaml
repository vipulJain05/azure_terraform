name: Build and push Docker image

on:
  push:
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Login to Quay.io
      uses: docker/login-action@v1
      with:
        registry: quay.io
        username: ${{ secrets.QUAY_USERNAME }}
        password: ${{ secrets.QUAY_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: server/
        push: true
        tags: quay.io/vipul05/azure_learning:latest


# - name: Build, tag, and push image to Amazon ECR
#       env:
#         ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#         ECR_REPOSITORY: export-generator
#         IMAGE_TAG: ${{ github.sha }}-${{ env.time }}
#       run: |
#         docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:development-$IMAGE_TAG -f exportGeneratorJs/Dockerfile .
#         docker push $ECR_REGISTRY/$ECR_REPOSITORY:development-$IMAGE_TAG